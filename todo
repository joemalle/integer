general:
	would be nice to work on more platforms
	don't copy so much

bugs:
	something wrong without fsanitize address

bad algos:
	/= and %= are O(nlogn) but should be O(n)

missing:
	sqrt
	pow
	search for assert(false) ... some codepaths missing
